import { useState } from 'react';
import { team } from '../content';
import { Arrow } from '../components/ui';
export default function Contact(){const [message,setMessage]=useState('');async function copy(){try{await navigator.clipboard.writeText(team.email);setMessage('邮箱已复制');}catch{setMessage('请选中上方邮箱并复制');}}return <section className="section page contact-page"><div className="contact-heading"><h1>LET’S<br/>SHAPE<br/>THE NIGHT.</h1><p>通过光，创造下一个空间故事。</p></div><div className="contact-details"><h2>项目咨询</h2><div className="email-row"><a href={`mailto:${team.email}`} className="email">{team.email}</a><button className="outline-button" onClick={copy}>复制邮箱</button></div><a className="text-link" href={`mailto:${team.email}?subject=${encodeURIComponent('建筑照明项目咨询')}`}>撰写邮件<Arrow/></a><p className="contact-phone">办公电话 <a href={`tel:${team.phone.replaceAll(' ','')}`}>{team.phone}</a></p><small>联系方式源自 2023 年团队画册</small><span className="copy-status" role="status">{message}</span></div></section>;}
