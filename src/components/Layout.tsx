import { useEffect,useState } from 'react';
import { Link,NavLink,useLocation } from 'react-router-dom';
import { team } from '../content';
const nav=[['/work','作品'],['/about','专业与团队'],['/lab','光的实验室'],['/contact','联系']];
export function Header(){const [open,setOpen]=useState(false);const {pathname}=useLocation();useEffect(()=>setOpen(false),[pathname]);useEffect(()=>{if(!open)return;const f=(e:KeyboardEvent)=>{if(e.key==='Escape')setOpen(false);};document.addEventListener('keydown',f);return()=>document.removeEventListener('keydown',f);},[open]);return <><a className="skip-link" href="#main">跳到主要内容</a><header className={`site-header ${open?'menu-open':''}`}><Link to="/" className="brand" aria-label="TJAD 建筑照明所首页">TJAD <span>/</span> <b>建筑照明所</b></Link><button className="menu-toggle" aria-expanded={open} aria-controls="primary-nav" onClick={()=>setOpen(!open)}>{open?'关闭':'菜单'}<span>{open?'−':'+'}</span></button><nav id="primary-nav" className={open?'open':''} aria-label="主导航">{nav.map(([to,label])=><NavLink key={to} to={to} data-magnetic>{label}</NavLink>)}</nav></header></>;}
export function Footer(){return <footer className="site-footer"><div><Link to="/" className="footer-brand">TJAD / 建筑照明所</Link><p>{team.fullName}<br/>{team.department}</p></div><div className="footer-right"><span>LIGHT, AS ARCHITECTURE.</span><small>作品及团队资料源自 2023 年画册</small><a href="#main">返回顶部 ↑</a></div></footer>;}
